---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: api-server
  name: api-server
spec:
  selector:
    app: api-server
  ports:
  - name: grpc
    port: 9090
    protocol: TCP
    targetPort: 9090
  - name: http-and-grpc-web
    port: 8080
    protocol: TCP
    targetPort: 8080
